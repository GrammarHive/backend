# Server Configuration
SERVER_ADDR=:8080
ENV=development

# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017/resumes

# Grammar Service
DEFAULT_GRAMMAR_NAME=resume

# Optional: Cloud Provider Settings (if using MongoDB Atlas)
# MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/resumes?retryWrites=true&w=majority
