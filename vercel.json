{
  "version": 2,
  "builds": [
    {
      "src": "api/*.go",
      "use": "@vercel/go"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api"
    }
  ],
  "env": {
    "MONGO_URI": "@MONGO_URI",
    "SERVER_ADDR": "@SERVER_ADDR",
    "ENV": "@ENV"
  }
}
